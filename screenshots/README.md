# Validation Screenshots

Add sanitized screenshots using these filenames:

- `vpc-subnets.png` — VPC and six-subnet layout
- `asg-inservice.png` — two Auto Scaling instances in service
- `target-group-healthy.png` — two healthy registered targets
- `alb-http-200.png` — ALB health endpoint returning HTTP 200
- `rds-private.png` — RDS connectivity and security configuration
- `cloudwatch-alarm.png` — high-CPU CloudWatch alarm

Before publishing, crop or blur:

- AWS account ID
- User or role names where unnecessary
- Database endpoints
- Resource identifiers that are not needed as evidence
- Passwords, access keys, secret keys, session tokens, and MFA codes

Never upload credentials or authentication codes.
